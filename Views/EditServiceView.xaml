<UserControl x:Class="PhotoStudioApp.Views.EditServiceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PhotoStudioApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="400">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/ThemeColor.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Background="{StaticResource Panel}">
        <StackPanel>
            <Label Content="Редактирование услуги" HorizontalAlignment="Center" FontSize="22" FontWeight="DemiBold" Foreground="{StaticResource Text}"/>

            <Label Content="Название услуги" FontSize="16" FontWeight="Medium" Foreground="{StaticResource Text}"/>
            <TextBox Name="ServiceNameBox" FontSize="16" Width="200" HorizontalAlignment="Left" Margin="5 0 0 0" PreviewTextInput="ServiceNameBox_PreviewTextInput"/>

            <Label Content="Описание услуги" FontSize="16" FontWeight="Medium" Foreground="{StaticResource Text}"/>
            <TextBox Name="DescriptionBox" FontSize="16" Height="80" Width="250" HorizontalAlignment="Left" Margin="5 0 0 0" PreviewTextInput="ServiceNameBox_PreviewTextInput"/>

            <Label Content="Стоимость услуги" FontSize="16" FontWeight="Medium" Foreground="{StaticResource Text}"/>
            <TextBox Name="CostBox" FontSize="16" Width="200" HorizontalAlignment="Left" Margin="5 0 0 0" PreviewTextInput="CostBox_PreviewTextInput"/>

            <StackPanel Orientation="Horizontal" Margin="5 10 0 0">
                <Button Content="Сохранить" Width="90" Margin="0 0 20 0" Background="{StaticResource ButtonColor}" Foreground="{StaticResource Text}" FontSize="15" Click="Button_Click_1"/>
                <Button Content="Закрыть" Width="90" Background="{StaticResource ButtonColor}" Foreground="{StaticResource Text}" FontSize="15" Click="Button_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
