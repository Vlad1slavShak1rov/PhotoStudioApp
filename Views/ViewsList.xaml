<UserControl x:Class="PhotoStudioApp.Views.ViewsList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PhotoStudioApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/ThemeColor.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="{StaticResource Text}" />

        <StackPanel Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Center">
            <Button Name="MakeViewsButton" Margin ="20 0 0 0" Width ="120" Content="Оставить отзыв"  Background="{DynamicResource BackgroundColor}" Foreground="{StaticResource add}" Click="MakeViewsButton_Click"/>
        </StackPanel>
        
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="0">
            <StackPanel Margin="0 0 50 0">
                <TextBlock Text="Сортировка" HorizontalAlignment="Center"/>
                <ComboBox Name="FilteredComboBox" Width="150" SelectedIndex="0" SelectionChanged="ComboBox_SelectionChanged">
                    <ComboBoxItem Content="Не сортировать"/>
                    <ComboBoxItem Content="Сначала новые"/>
                    <ComboBoxItem Content="Сначала старые"/>
                    <ComboBoxItem Content="С высокой оценкой"/>
                    <ComboBoxItem Content="С низкой оценкой"/>
                </ComboBox>
            </StackPanel>
        </StackPanel>
        <Grid x:Name="MainGrid" Grid.Row="1">
            <ScrollViewer Name="MainScrollViewer" Grid.Row="1">
                <StackPanel Name="MainPanel">

                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
