<UserControl x:Class="PhotoStudioApp.Views.AddWorkerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PhotoStudioApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="320" d:DesignWidth="450">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/ThemeColor.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid Background="{StaticResource Panel}">
       
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Content="Добавление сотрудника" FontSize="24" FontWeight="Bold" VerticalAlignment="Top" HorizontalAlignment="Center" Foreground="{StaticResource Text}"/>
        <Button Name="BackButton" HorizontalAlignment="Left" VerticalAlignment ="Center" Width="30" Height="30" Content="&lt;&lt;" Click="BackButton_Click" Background="{StaticResource Panel}"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Row="1" Grid.Column="0">

                <Label Content="Логин" FontSize="16"/>
                <TextBox Name="LoginBox" Width="200" HorizontalAlignment="Left" Margin="5 0 0 0" FontSize="16" PreviewTextInput="LoginBox_PreviewTextInput"/>

                <Label Content="Пароль" FontSize="16"/>
                <PasswordBox Name="PasswordBoxOne" Width="200" HorizontalAlignment="Left" Margin="5 0 0 0" FontSize="16"/>

                <Label Content="Повторный пароль" FontSize="16"/>
                <PasswordBox Name="PasswordBoxTwo" Width="200" HorizontalAlignment="Left" Margin="5 0 0 0" FontSize="16"/>
        
            </StackPanel>

            <StackPanel Grid.Column="1">
                <Label Content="Фамилия"  FontSize="16"/>
                <TextBox Name="SecondNameBox" Width="200" HorizontalAlignment="Left" Margin="5 0 0 0" FontSize="16" PreviewTextInput="LoginBox_PreviewTextInput"/>

                <Label Content="Имя"  FontSize="16"/>
                <TextBox Name="NameBox" Width="200" HorizontalAlignment="Left" Margin="5 0 0 0" FontSize="16" PreviewTextInput="LoginBox_PreviewTextInput"/>

                <Label Content="Отчество"  FontSize="16"/>
                <TextBox Name="LastNameBox" Width="200" HorizontalAlignment="Left" Margin="5 0 0 0" FontSize="16" PreviewTextInput="LoginBox_PreviewTextInput"/>

                <Label Content="Должность" FontSize="16"/>
                <ComboBox Name="PostComboBox" Width="200" Margin="0 0 15 0" SelectedIndex="0" FontSize="16">
                    <ComboBoxItem Content="Фотограф"/>
                    <ComboBoxItem Content="Визажист"/>
                </ComboBox>

                <Button Name="SaveButton" Content="Сохранить" Width="75" HorizontalAlignment="Right" Margin="0 20 10 0" FontSize="14" Background="{StaticResource BackgroundColor}" Click="SaveButton_Click"/>
            </StackPanel>
        </Grid>
       
    </Grid>
</UserControl>
