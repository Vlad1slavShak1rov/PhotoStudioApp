<UserControl x:Class="PhotoStudioApp.Views.EditWorker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PhotoStudioApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/ThemeColor.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Background="{StaticResource Panel}">
        <StackPanel>
            <Label Content="Окно редактирования" HorizontalAlignment="Center" FontSize="20" FontWeight="ExtraBold" Foreground="{StaticResource add}"/>
           
            <Label Content="Фамилия" FontSize="16" FontWeight="ExtraBlack" Foreground="{StaticResource add}"/>
            <TextBox Name="SecondNameBox" Width="200" Height="25" Margin="5 0 0 0" HorizontalAlignment="Left" FontSize="16" PreviewTextInput="SecondNameBox_PreviewTextInput"/>
            
             <Label Content="Имя" FontSize="16" FontWeight="ExtraBlack" Foreground="{StaticResource add}"/>
            <TextBox Name="NameBox" Width="200" Height="25" Margin="5 0 0 0" HorizontalAlignment="Left" FontSize="16" PreviewTextInput="SecondNameBox_PreviewTextInput"/>
            
             <Label Content="Отчество" FontSize="16" FontWeight="ExtraBlack" Foreground="{StaticResource add}"/>
            <TextBox Name="LastNameBox" Width="200" Height="25" Margin="5 0 0 0" HorizontalAlignment="Left" FontSize="16" PreviewTextInput="SecondNameBox_PreviewTextInput"/>
            
             <Label Content="Логин" FontSize="16" FontWeight="ExtraBlack" Foreground="{StaticResource add}"/>
            <TextBox Name="LoginBox" Width="200" Height="25" Margin="5 0 0 0" HorizontalAlignment="Left" FontSize="16" PreviewTextInput="SecondNameBox_PreviewTextInput"/>
            
             <Label Content="Пароль" FontSize="16" FontWeight="ExtraBlack" Foreground="{StaticResource add}"/>
            <TextBox Name="PasswordBox" Width="200" Height="25" Margin="5 0 0 0" HorizontalAlignment="Left" FontSize="16"/>

            <Button x:Name="SaveButton" Content="Сохранить" Width="120" Margin="0 20 0 10" Background="{StaticResource ButtonColor}" Foreground="{StaticResource Text}" FontSize="14" Click="SaveButton_Click"/>
            <Button x:Name="ExitButton" Content="Выход" Width="120" Background="{StaticResource ButtonColor}" Foreground="{StaticResource Text}" FontSize="14" Click="ExitButton_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
