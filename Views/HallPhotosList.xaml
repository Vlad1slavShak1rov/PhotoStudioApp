<UserControl x:Class="PhotoStudioApp.Views.HallPhotosList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PhotoStudioApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/ThemeColor.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="{StaticResource Text}" >
            <TextBlock Text="Залы" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="24" FontWeight="ExtraBlack" Foreground="{StaticResource add}"/>
            <Button Name="btAddHall" HorizontalAlignment="Left" Content="Добавить зал" Width="150" FontSize="16" Background="{StaticResource BackgroundColor}" Foreground="{StaticResource add}" Visibility="Visible" Click="btAddHall_Click"/>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <StackPanel x:Name="hallFiltrationPanel" Visibility="Visible" Grid.Column="0" Background="{StaticResource ButtonColor}">
                <Label Content="Зал" FontSize="16"/>
                <ComboBox x:Name="cbHalls" FontSize="16" SelectionChanged="cbHalls_SelectionChanged" SelectedIndex="0"/>
                <Button x:Name="btAddPhoto" Content="Добавить фото" Margin="10" Background="{StaticResource Text}" Click="btAddPhoto_Click"/>
            </StackPanel>

            <ScrollViewer Grid.Column="1">
                <WrapPanel  x:Name="imagePanel" HorizontalAlignment="Left" VerticalAlignment="Top">
                </WrapPanel>
            </ScrollViewer>
            
        </Grid>
    </Grid>
</UserControl>
