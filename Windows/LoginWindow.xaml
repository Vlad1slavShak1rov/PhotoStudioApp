<Window x:Class="PhotoStudioApp.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PhotoStudioApp"
        mc:Ignorable="d"
        Title="Авторизация" Height="450" Width="800" WindowStartupLocation="CenterScreen" MinHeight="450" MinWidth="800">
    <Window.Resources>
        <ResourceDictionary Source="/Resources/ThemeColor.xaml" />
    </Window.Resources>
    <Grid Background="{StaticResource BackgroundColor}">
        <StackPanel VerticalAlignment="Center">
            <Label Content="Фотостудия" HorizontalAlignment="Center" FontSize="32" Foreground="{StaticResource Panel}" FontWeight="SemiBold"/>
            <Border  HorizontalAlignment="Center" VerticalAlignment="Center" Height="200" Width="250" CornerRadius="5">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Логин" HorizontalAlignment="Center" FontSize="14" Foreground="{StaticResource Panel}" FontWeight="UltraBold"/>
                    <TextBox Name="LoginBox" Width="175" HorizontalAlignment="Center" Margin="10 0 0 10" FontSize="14"/>

                    <TextBlock Text="Пароль" HorizontalAlignment="Center" FontSize="14" Foreground="{StaticResource Panel}" FontWeight="UltraBold"/>
                    <PasswordBox Name="PasswordBox" Width="175" HorizontalAlignment="Center" Margin="10 0 0 10" FontSize="14"/>

                    <Button Name="SingInButton" Content="Войти" Width ="120" Margin = "0 0 0 10" Background="{StaticResource ButtonColor}" Foreground="{StaticResource Text}" Click="SingInButton_Click"/>
                    <Button Name="SignUpButton" Content="Зарегистрироваться" Width ="120" Background="{StaticResource ButtonColor}" Foreground="{StaticResource Text}" Click="SignUpButton_Click"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</Window>
